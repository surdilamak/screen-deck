node_modules/
dist/
out/
*.log
.DS_Store

# Local agent tooling (third-party skill — not app code)
.claude/

# npm lockfile — regenerated per-platform, caused cross-machine pull conflicts
package-lock.json
